import 'package:flutter/material.dart';

class MovieRanking extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("ranking")),
    );
  }
}